mutation StartExperiment($input: StartExperimentInput!) {
  startExperiment(input: $input) {
    experiment {
      id
      productName
      startDate
    }
    errors {
      ...StarndardError
    }
  }
}
